extends layout

block content
    .main
        h1 Pushhub

        each repo in repos
           .repo
                h3
                    a(href= basepath + '/' + repo.name) #{repo.name}
                p Last updated #{repo.mtime}
