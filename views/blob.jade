extends layout

block content
    div.preview
        if mime.indexOf('image') == 0
            img(src=rawURL)
        else
            pre(data-extension=extension, class='code')
                =data.toString()
